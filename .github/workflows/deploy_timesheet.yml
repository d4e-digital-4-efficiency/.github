name: Deploy Timesheet Workflow

on:
  workflow_dispatch:
    inputs:
      repos:
        description: 'Repos à cibler (séparés par des virgules)'
        required: true
        default: 'welcome,LacavedeGeneveSA'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Validate required secret
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          if [ -z "$GH_PAT" ]; then
            echo "GH_PAT is required for cross-repository deployment."
            exit 1
          fi

      - name: Checkout repo central
        uses: actions/checkout@v3
        with:
          repository: ${{ github.repository_owner }}/.github
          token: ${{ secrets.GH_PAT }}
          ref: main

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Deploy workflow to repos
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          ORG_NAME: ${{ github.repository_owner }}
          REPOS: ${{ github.event.inputs.repos }}
        run: python .github/scripts/deploy_timesheet.py
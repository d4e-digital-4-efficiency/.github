name: Deploy Timesheet Workflow (All Repos)

on:
  workflow_dispatch:
    inputs:
      exclude:
        description: 'Repos à exclure en plus (séparés par des virgules)'
        required: false
        default: ''

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Validate required secret
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          if [ -z "$GH_PAT" ]; then
            echo "GH_PAT is required for cross-repository deployment."
            exit 1
          fi

      - name: Checkout repo central
        uses: actions/checkout@v3
        with:
          repository: ${{ github.repository_owner }}/.github
          token: ${{ secrets.GH_PAT }}
          ref: main

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Deploy workflow to all repos
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          ORG_NAME: ${{ github.repository_owner }}
          EXTRA_EXCLUDE: ${{ github.event.inputs.exclude }}
        run: python .github/scripts/deploy_timesheet_all.py